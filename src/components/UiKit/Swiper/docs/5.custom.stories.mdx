import { ref, onMounted } from 'vue'
import { Meta, Story, Canvas, ArgsTable, Description, Source } from '@storybook/addon-docs';
import { WithApp, scrollable } from 'decorators'
import UiSwiper from '../index.vue'
import Basic from './Basic.vue'

<Meta title='Components / UiKit / Swiper / 5. Custom' component={UiSwiper} decorators={[WithApp, scrollable]}/>

<Description markdown={UiSwiper.__docgenInfo.docsBlocks[0]} />


## Custom
every default swiperjs module is ready to receive our project styling.

<Canvas>
  <Story name='5. Custom'
         args={{
          tag: 'div',
          options: {
            slidesPerView: 'auto',
          },
          pagination: true,
          navigation: true,
          autoplay: true,
          progressBar: true,
          scrollbar: true,
          slidesPerView: 'auto',
          spaceBetween: 50,
          centeredSlides: false,
          loop: false,
          onChange: false,
          watchEnd: false,
          cursor: true,
          overflow: true,
          animated: true,
          animation: undefined,
         }}
  >
    { ((args) => ({
      components: { Basic },
      setup() {
        return { args }
      },
      template: `<Basic :key="args" :options="args"></Basic>`,
    })).bind({}) }
  </Story>
</Canvas>


## Props
<ArgsTable of={UiSwiper} />
