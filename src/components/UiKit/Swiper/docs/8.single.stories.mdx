import { ref, onMounted } from 'vue'
import { Meta, Story, Canvas, ArgsTable, Description, Source } from '@storybook/addon-docs';
import { WithApp, scrollable } from 'decorators'
import UiSwiper from '../index.vue'
import SingleSlide from './SingleSlide.vue'

<Meta title='Components / UiKit / Swiper / 8. Single' component={UiSwiper} decorators={[WithApp, scrollable]}/>

<Description markdown={UiSwiper.__docgenInfo.docsBlocks[0]} />


## Single
Simple example of slider with 1 slide changing with basic animation

<Canvas>
  <Story name='8. Single'
  >
    { (() => ({
      components: { SingleSlide },
      setup() {
      },
      template: `<SingleSlide></SingleSlide>`,
    })).bind({}) }
  </Story>
</Canvas>


## Props
<ArgsTable of={UiSwiper} />
