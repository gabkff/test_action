import { ref, onMounted } from 'vue'
import { map } from 'lodash'
import { Meta, Story, Canvas, ArgsTable, Description, Source } from '@storybook/addon-docs';
import { WithApp, scrollable } from 'decorators'
import { image } from 'assets/moc' 
import UiSwiper from '../index.vue'
import Layout from './Layout.vue'

<Meta title='Components / UiKit / Swiper / 6. Layout' component={UiSwiper} decorators={[WithApp, scrollable]}/>

<Description markdown={UiSwiper.__docgenInfo.docsBlocks[0]} />


## Layout
just a classic exemple of what we can do it easily with our swiper.

<Canvas>
  <Story name='6. Layout'
         args={{
          tag: 'div',
          options: {
            slidesPerView: 'auto',
          },
          pagination: false,
          autoplay: false,
          progressBar: false,
          scrollbar: false,
          slidesPerView: 'auto',
          spaceBetween: 0,
          centeredSlides: false,
          loop: false,
          onChange: false,
          watchEnd: false,
          cursor: true,
          overflow: true,
          animated: true,
          animation: undefined,
          title: 'With left title',
          description: 'Title + nav should be on left on desktop<br>on mobile - swiper is stacked - align with content and overflow visible',
          items: [
            {
              title:'some random title',
              image,
              href:'#',
              description: '<p>with a small description</p>'
            },
            {
              title:'some random title',
              image,
              href:'#',
              description: '<p>with a small description</p>'
            },
            {
              title:'some random title',
              image,
              href:'#',
              description: '<p>with a small description</p>'
            },
            {
              title:'some random title',
              image,
              href:'#',
              description: '<p>with a small description</p>'
            },
          ]
         }}
  >
    { ((args) => ({
      components: { Layout },
      setup() {
        return { args }
      },
      template: `<Layout :key="args" :options="args" :title="args.title" :description="args.description" :items="args.items"></Layout>`,
    })).bind({}) }
  </Story>
</Canvas>


## Props
<ArgsTable of={UiSwiper} />
