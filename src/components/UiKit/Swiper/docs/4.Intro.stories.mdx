import { ref, onMounted } from 'vue'
import { Meta, Story, Canvas, ArgsTable, Description, Source } from '@storybook/addon-docs';
import { WithApp, scrollable } from 'decorators'
import UiSwiper from '../index.vue'
import Intro from './Intro.vue'

<Meta title='Components / UiKit / Swiper / 4. Intro' component={UiSwiper} decorators={[WithApp, scrollable]}/>

<Description markdown={UiSwiper.__docgenInfo.docsBlocks[0]} />


## Intro
by default there is a basic intro animation that we can disable or even pass a new gsap timeline / tween

<Canvas>
  <Story name='4. Intro'
         args={{
          tag: 'div',
          options: {
            slidesPerView: 'auto',
          },
          pagination: false,
          navigation: false,
          autoplay: false,
          progressBar: false,
          scrollbar: false,
          slidesPerView: 'auto',
          spaceBetween: 50,
          centeredSlides: false,
          loop: false,
          onChange: false,
          watchEnd: false,
          cursor: true,
          overflow: true,
          animated: true,
          animation: undefined,
         }}
  >
    { ((args) => ({
      components: { Intro },
      setup() {
        return { args }
      },
      template: `<Intro :key="args" :options="args"></Intro>`,
    })).bind({}) }
  </Story>
</Canvas>


## Props
<ArgsTable of={UiSwiper} />
