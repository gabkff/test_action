import { ArgsTable, Canvas, Description, Meta, Source, Story } from '@storybook/addon-docs';
import { WithApp, scrollable } from 'decorators';
import { onMounted, ref } from 'vue';
import UiSwiper from '../index.vue';
import Basic from './Basic.vue';

<Meta title='Components / UiKit / Swiper / 1. Basic' component={UiSwiper} decorators={[WithApp, scrollable]}/>

<Description markdown={UiSwiper.__docgenInfo.docsBlocks[0]} />


## Basic
Can have anything as a slot, each child element will became a slide.
Also possible to use the web component version (https://swiperjs.com/element#parameters-as-props)

<Canvas>
  <Story name='1. Basic'
         args={{
          tag: 'div',
          options: {
            slidesPerView: 'auto',
          pagination: false,
          navigation: false,
          autoplay: false,
          progressBar: false,
          scrollbar: false,
          slidesPerView: 'auto',
          spaceBetween: 50,
          centeredSlides: false,
          loop: false,
          onChange: false,
          watchEnd: false,
          cursor: true,
          overflow: false,
          animated: true,
          animation: undefined,
          },
         }}
  >
    { ((args) => ({
      components: { Basic },
      setup() {
        return { args }
      },
      template: `<Basic :key="args" :options="args"></Basic>`,
    })).bind({}) }
  </Story>
</Canvas>


## Props
<ArgsTable of={UiSwiper} />
