import { ref, onMounted } from 'vue'
import { Meta, Story, Canvas, ArgsTable, Description, Source } from '@storybook/addon-docs';
import { WithApp, scrollable } from 'decorators'
import UiSwiper from '../index.vue'
import Navigation from './Navigation.vue'

<Meta title='Components / UiKit / Swiper / 3. Navigation' component={UiSwiper} decorators={[WithApp, scrollable]}/>

<Description markdown={UiSwiper.__docgenInfo.docsBlocks[0]} />


## Navigation
we can easily overwritte overwritte the nav system. it will actually still use the default swiper system.

<Canvas>
  <Story name='3. Navigation'
         args={{
          tag: 'div',
          options: {
            slidesPerView: 'auto',
          },
          pagination: false,
          navigation: false,
          autoplay: false,
          progressBar: false,
          scrollbar: false,
          slidesPerView: 'auto',
          spaceBetween: 50,
          centeredSlides: false,
          loop: false,
          onChange: false,
          watchEnd: false,
          cursor: true,
          overflow: true,
          animated: true,
          animation: undefined,
         }}
  >
    { ((args) => ({
      components: { Navigation },
      setup() {
        return { args }
      },
      template: `<Navigation :key="args" :options="args"></Navigation>`,
    })).bind({}) }
  </Story>
</Canvas>


## Props
<ArgsTable of={UiSwiper} />
