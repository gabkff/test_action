import { ArgsTable, Canvas, Description, Meta, Source, Story } from '@storybook/addon-docs';
import { WithApp, scrollable } from 'decorators';
import 'swiper/css';
import 'swiper/css/effect-creative';
import { onMounted, ref } from 'vue';
import UiSwiper from '../index.vue';
import Basic from './Basic.vue';

<Meta title='Components / UiKit / Swiper / 2. Overflow' component={UiSwiper} decorators={[WithApp, scrollable]}/>

<Description markdown={UiSwiper.__docgenInfo.docsBlocks[0]} />


## Overflow
with overflow activated, we will see the non current slide overlap his container (can be usefull with specific design)
make sure that you handle that overflow at some point, on the block, the page or whatever.

<Canvas>
  <Story name='2. Overflow'
         args={{
          tag: 'div',
          options: {
            slidesPerView: 'auto',
          },
          pagination: false,
          navigation: false,
          autoplay: false,
          progressBar: false,
          scrollbar: false,
          slidesPerView: 'auto',
          spaceBetween: 50,
          centeredSlides: false,
          loop: false,
          onChange: false,
          watchEnd: false,
          cursor: true,
          overflow: true,
          animated: true,
          animation: undefined,
         }}
  >
    { ((args) => ({
      components: { Basic },
      setup() {
        return { args }
      },
      template: `<Basic :key="args" :options="args"></Basic>`,
    })).bind({}) }
  </Story>
</Canvas>


## Props
<ArgsTable of={UiSwiper} />
